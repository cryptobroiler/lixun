/target
Cargo.lock

# Arch packaging build output (do not commit — artifacts are rebuilt)
/packaging/arch/pkg/
/packaging/arch/src/
/packaging/arch/*.pkg.tar.zst
/packaging/arch/lixun-*.tar.gz
/packaging/arch/lupa-*.tar.gz
/packaging/arch/releases/lixun-*/
/packaging/arch/releases/lupa-*/

# local tooling agent workspace (plans, notepads, transcripts — local only)
/.local/

# AGENTS.md — project-level instructions for AI coding agents,
# local-only per user preference (do not distribute with the repo).
/AGENTS.md

# Local reference documents (PDFs, etc.) used for design research
/docs/references/

