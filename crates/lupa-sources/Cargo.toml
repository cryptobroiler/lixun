[package]
name = "lupa-sources"
version = "0.1.0"
edition = "2024"

[dependencies]
lupa-core = { path = "../lupa-core" }
lupa-extract = { path = "../lupa-extract" }
anyhow = "1.0"
walkdir = "2.5"
notify = "7.0"
tracing = "0.1"
tokio = { version = "1", features = ["time", "fs"] }
